<Page x:Class="GB_ServerManager.Views.Servers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GB_ServerManager.Views"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Servers">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="40" />
        </Grid.ColumnDefinitions>

        <TabControl Name="tbcServerList" Background="#555346" BorderThickness="0" TabStripPlacement="Left">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" CornerRadius="4" Padding="5,0,0,0">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Left" ContentSource="Header" 
                                                      Height="28" Width="100" Margin="0" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#626050" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#555346" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Header}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                        <StackPanel Orientation="Vertical">
                            <Label Content="Server Name:" />
                            <Label Content="Server Path:" />
                            <Label Content="Server Port:" />
                            <Label Content="Query Port:" />
                            <Label Content="Restart Time:" />
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBox x:Name="tbxSrvrName" Text="{Binding ServerName}" Width="200" Margin="0,4,0,4"/>
                            <TextBox x:Name="tbxSrvrPath" Text="{Binding ServerPath}" Width="200" Margin="0,4,0,4"/>
                            <TextBox x:Name="tbxServerPort" Text="{Binding Port}"  Width="20" Margin="0,4,0,4" HorizontalAlignment="Left"/>
                            <TextBox x:Name="tbxQueryPort" Text="{Binding QueryPort}" Width="20" Margin="0,4,0,4" HorizontalAlignment="Left"/>
                            <xctk:IntegerUpDown x:Name="tbxRestartTime" Value="{Binding RestartTime}" Width="60" Height="30" Maximum="24" Minimum="0" DefaultValue="12" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <Button Name="btnAddExisting" Grid.Column="1" Height="40" VerticalAlignment="Top" FontFamily="Segoe MDL2 Assets" Background="Transparent" BorderThickness=".5" Click="btnAddExisting_Click">
            &#xE8E5;
            <Button.ToolTip>
                <ToolTip Content="Add existing server" />
            </Button.ToolTip>
        </Button>
        
    </Grid>
</Page>

<!--public string Header { get; set; }
        public string ServerBasePath { get; set; }
        public string ServerName { get; set; }
        public string ServerPath { get; set; }
        public string ServerMOTD { get; set; }
        public string ServerPassword { get; set; }
        public int Port { get; set; }
        public int QueryPort { get; set; }
        public int RestartTime { get; set; }
        public int MaxPlayers { get; set; }
        public int MaxSpectators { get; set; }
        public string GameRules { get; set; }-->
